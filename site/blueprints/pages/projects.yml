title: Projects
sections:
  drafts:
    headline: Draft project
    type: pagetable
    status: all
    limit: 25
    templates:
      - project
    columns:
      title:
        label: Title
        text: '{{ page.title }}'
        searchable: true
      category:
        label: Category
        text: '{{ page.category }}'
        searchable: true
      year:
        label: Year
        text: '{{ page.year }}'
